RHOAI instance chart has been installed. It deploys:

  - DSCInitialization (default-dsci)
  - DataScienceCluster (default-dsc)
  - OdhDashboardConfig (odh-dashboard-config)
  - HardwareProfile(s) when hardwareProfiles.enabled is true (e.g. gpu-profile)

Toggle components via values: featureStore, modelsAsService, distributedWorkloads,
ogx, modelRegistry, mlflow, trustyai. Add more profiles under hardwareProfiles.profiles.

Validate with: helm template . -f values.yaml
